namespace Bookshelf.Domain.Constants;

public static class ErrorCodes
{
    public static readonly string INVALID_AUTHORS = nameof(INVALID_AUTHORS);
    public static readonly string INVALID_ISBN = nameof(INVALID_ISBN);
    public static readonly string INVALID_PUBLISHER = nameof(INVALID_PUBLISHER);
    public static readonly string INVALID_TITLE = nameof(INVALID_TITLE);
}
